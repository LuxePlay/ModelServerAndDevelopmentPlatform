# 三级权限系统设计方案
## 前端设计流程
### 权限控制机制设计
前端需要实现基于角色的访问控制（RBAC）机制，包括：
#### 路由权限控制：
-  在路由配置中添加权限字段（已经部分实现）
- 创建权限控制高阶组件或自定义Hook
- 根据用户权限动态渲染可访问的路由
#### 菜单权限控制：
- 根据用户权限动态渲染侧边栏和顶部导航菜单
- 不同角色显示不同的菜单项
#### 页面元素权限控制：
- 在页面内根据权限控制按钮、表单等元素的显示与隐藏
- 实现细粒度的权限控制
#
### 状态管理设计
#
#### 用户信息存储：
- 使用Redux或Context API存储用户信息和权限
- 包括用户角色、权限列表、访问令牌等
#### 权限验证逻辑：
- 创建权限验证函数，判断用户是否有访问特定资源的权限
- 实现权限检查的自定义Hook
#### Token刷新机制设计
- 实现访问令牌和刷新令牌双令牌机制
- 添加令牌过期自动刷新逻辑
- 处理多标签页下的Token同步问题
#### 增加权限缓存机制
- 实现权限信息的本地存储（localStorage/sessionStorage）
- 添加权限缓存更新机制
- 处理权限变更时的缓存同步
#### 增加错误处理和权限拒绝页面
- 创建权限拒绝页面（403页面）
- 统一处理权限验证失败的情况
- 提供友好的错误提示
### 具体实现方案
#### 增强路由权限控制：
- 修改路由配置，为每个路由添加具体的权限标识
- 创建权限路由组件，根据用户权限渲染对应路由
#### 登录认证流程：
- 用户登录时获取用户角色和权限信息
- 将权限信息存储到全局状态管理中
- 根据权限信息动态渲染界面
#### 菜单权限控制：
- 修改侧边栏组件，根据用户权限过滤菜单项
- 为不同角色创建不同的菜单配置
#
## 后端设计流程
### 数据库设计
需要设计以下数据表：
#### 用户表（users）：
- id: 用户ID
- username: 用户名
- student_id: 学号
- phone: 手机号
- department: 院系
- real_name: 真实姓名
- password: 密码（加密存储）
- role_id: 角色ID（关联角色表）
- email: 邮箱
- status: 用户状态（active/inactive）
- created_at: 创建时间
- updated_at: 更新时间
#### 角色表（roles）：
- id: 角色ID
- name: 角色名称（super_admin, admin, user）
- description: 角色描述
- level: 角色级别（1-超级管理员，2-管理员，3-普通用户）
- status: 角色状态
- created_at: 创建时间
- updated_at: 更新时间
#### 权限表（permissions）：
- id: 权限ID
- name: 权限名称
- description: 权限描述
- module: 所属模块（如：课程管理、成绩管理、实验室预约等）
- action: 操作类型（create/read/update/delete）
- resource: 资源标识
- created_at: 创建时间
#### 院系表（departments）：
- id: 院系ID
- name: 院系名称
- code: 院系代码
#### 专业表（majors）：
- id: 专业ID
- name: 专业名称
- department_id: 所属院系
#### 班级表（classes）：
- id: 班级ID
- name: 班级名称
- major_id: 所属专业
- grade: 年级
#### 用户班级关联表（user_classes）：
- user_id: 用户ID
- class_id: 班级ID
- type: 类型（student代表学生，advisor代表班主任）
#### 角色权限关联表（role_permissions）：
- role_id: 角色ID
- permission_id: 权限ID
#### API安全设计
- 实现API访问频率限制（Rate Limiting）
- 添加请求签名验证
- 实现数据权限控制（行级/列级权限）
#### 日志审计设计
- 用户登录日志记录
- 权限变更操作日志
- 敏感操作审计日志
### API接口设计
#### 认证接口：
- POST /auth/login - 用户登录，返回用户信息和权限
- POST /auth/logout - 用户登出
#### 权限管理接口：
- GET /roles - 获取所有角色（仅超级管理员）
- GET /roles/:id - 获取角色详情（仅超级管理员）
- POST /roles - 创建角色（仅超级管理员）
- PUT /roles/:id - 更新角色（仅超级管理员）
- DELETE /roles/:id - 删除角色（仅超级管理员）
- GET /users - 获取用户列表（管理员及以上）
- GET /users/:id - 获取用户详情（管理员及以上）
- POST /users - 创建用户（管理员及以上）
- PUT /users/:id - 更新用户（管理员及以上）
- DELETE /users/:id - 删除用户（管理员及以上）
#### 权限验证接口：
- GET /permissions/check - 检查用户是否有特定权限
###  中间件设计
#### 认证中间件：
- 验证JWT令牌有效性
- 解析用户信息并附加到请求对象
#### 权限验证中间件：
- 检查用户是否具有访问特定资源的权限
- 根据不同角色限制访问
### 业务逻辑实现
#### 登录认证逻辑：
- 验证用户名和密码
- 查询用户角色和权限
- 生成JWT令牌返回给前端
#### 权限验证逻辑：
- 根据用户角色查询对应权限
- 提供权限检查接口
#
## 具体实现步骤
#
### 前端实现步骤：
#
#### 权限缓存和刷新机制：
- 添加权限信息本地存储
- 实现权限过期检查和刷新逻辑
#### 创建权限拒绝页面：
- 设计403 Forbidden页面
- 实现权限验证失败的统一跳转
#### 创建权限管理Context：
- 创建AuthContext存储用户信息和权限
- 实现登录、登出方法
#### 增强路由权限控制：
- 创建ProtectedRoute组件，根据权限渲染路由
- 修改路由配置，完善权限标识
#### 实现菜单权限控制：
- 修改侧边栏组件，根据权限过滤菜单项
- 修改顶部导航组件，根据权限过滤导航项
#### 实现页面元素权限控制：
- 创建权限检查自定义Hook
- 在页面中使用Hook控制元素显示
#
### 后端实现步骤：
#
#### 创建数据库表结构：
- 创建用户、角色、权限相关表
#### 实现认证模块：
- 创建JWT令牌生成和验证逻辑
- 实现登录接口
#### 实现权限管理模块：
- 实现角色和权限的增删改查接口
- 实现用户权限分配接口
#### 实现权限验证中间件：
- 创建权限检查中间件
- 在需要权限验证的接口上应用中间件
#### 日志审计模块：
- 创建操作日志记录中间件
- 实现敏感操作日志记录
#### 实现安全防护措施：
- 添加CSRF防护
- 实现CORS配置
- 添加SQL注入防护
#
### 测试
#
#### 前端测试：
- 权限路由拦截测试
- 菜单渲染测试
- 页面元素权限控制测试
#### 后端测试：
- 权限接口测试
- 安全防护测试
- 性能压力测试
### 部署和运维
#### 部署配置：
- 不同环境的权限配置管理
- 权限数据的初始化脚本
#### 监控告警：
- 权限异常访问监控
- 用户行为分析
#
## 总结
#
这个三级权限系统的设计方案包括：
## 前端：
- 基于角色的路由权限控制
- 动态菜单渲染
- 细粒度的页面元素权限控制
- 全局状态管理用户权限信息
## 后端：
- 完整的RBAC数据模型
- 认证和权限验证API
- JWT令牌机制
- 权限验证中间件